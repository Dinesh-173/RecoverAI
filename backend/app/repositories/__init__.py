"""Persistence helpers. Phase 2 uses SQLAlchemy models + sessions directly.

This package exists so the Phase 1 folder contract is stable. Query logic
belongs in services until a dedicated repository layer is introduced.
"""
